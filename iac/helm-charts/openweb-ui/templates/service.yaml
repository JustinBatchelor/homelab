---
apiVersion: v1
kind: Service
metadata:
  name: open-webui
  namespace: openweb-ui
spec:
  selector:
    app: open-webui
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 8080
  type: ClusterIP