---
kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: open-webui
  namespace: openweb-ui
spec:
  host: "open-webui.apps.sno.batchelor.live"
  to:
    kind: Service
    name: open-webui
    weight: 100
  port:
    targetPort: 8080
  tls:
    termination: edge
  wildcardPolicy: None