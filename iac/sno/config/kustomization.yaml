apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # install cert-manager
  - ../operators/cert-manager/
  # install rh-sso
  - ../operators/rh-sso/
  # install vault-secrets
  - ../operators/vault-secrets/
  # install ansible-automation-platform
  - ../operators/aap/
  # install openshift-virtualization
  # - ../operators/ocp-virt/
  # install lvm-storage
  # - ../operators/lvm-storage/